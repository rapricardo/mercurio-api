# =============================================================================
# 🚀 MERCURIO API - PORTAINER ENVIRONMENT VARIABLES
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores

# =============================================================================
# 🔐 REDIS CONFIGURATION
# =============================================================================
MERCURIO_REDIS_PASSWORD=your_redis_password_here

# =============================================================================
# 🗄️ SUPABASE DATABASE CONFIGURATION
# =============================================================================
# URL de conexão do Supabase (PostgreSQL)
SUPABASE_DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?schema=public

# Supabase Integration
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
SUPABASE_ANON_KEY=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...

# =============================================================================
# 🔐 ENCRYPTION & SECURITY (OBRIGATÓRIAS)
# =============================================================================
# Gere com: openssl rand -base64 32
ENCRYPTION_KEK_SECRET=your-base64-encoded-32-byte-master-key
EMAIL_DEK_SECRET=your-base64-encoded-email-encryption-key
PHONE_DEK_SECRET=your-base64-encoded-phone-encryption-key

# Gere com: openssl rand -hex 32
EMAIL_FINGERPRINT_SECRET=your-hex-encoded-email-fingerprint-secret
PHONE_FINGERPRINT_SECRET=your-hex-encoded-phone-fingerprint-secret

# =============================================================================
# 🔑 JWT CONFIGURATION
# =============================================================================
# Gere com: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-base64

# =============================================================================
# 🌐 CORS & NETWORK SETTINGS
# =============================================================================
CORS_ORIGIN=https://n8n.ricardotocha.com.br,https://evolution.ricardotocha.com.br,https://apollo.ricardotocha.com.br

# =============================================================================
# 📊 OPTIONAL SETTINGS
# =============================================================================
# Deixe em branco para usar defaults

# Log Level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=1000

# Data Retention
DEFAULT_DATA_RETENTION_DAYS=90

# =============================================================================
# 🚀 GERAÇÃO RÁPIDA DE CHAVES
# =============================================================================
# Execute estes comandos para gerar as chaves necessárias:
#
# ENCRYPTION_KEK_SECRET:
# openssl rand -base64 32
#
# EMAIL_DEK_SECRET:
# openssl rand -base64 32
#
# PHONE_DEK_SECRET:
# openssl rand -base64 32
#
# EMAIL_FINGERPRINT_SECRET:
# openssl rand -hex 32
#
# PHONE_FINGERPRINT_SECRET:
# openssl rand -hex 32
#
# JWT_SECRET:
# openssl rand -base64 32
#
# MERCURIO_REDIS_PASSWORD:
# openssl rand -base64 16